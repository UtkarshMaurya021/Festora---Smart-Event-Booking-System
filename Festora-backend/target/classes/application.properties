spring.application.name=Festora-backend
server.port=${PORT:8080}

# Database Credentials (Loaded dynamically from .env / System Environment)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/festora}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root123}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=25MB

# Razorpay API Credentials (Loaded dynamically from .env / System Environment)
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_TIuNseQI3AsTL4}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:7adHuxgXMcT0mkCAESNrUYmc}

# Real-Time Email Notification Credentials (Loaded dynamically from .env / System Environment)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:kalashsatypal4@gmail.com}
spring.mail.password=${MAIL_PASSWORD:cabzmqfsejgyaazr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000